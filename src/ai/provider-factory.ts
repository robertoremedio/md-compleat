// Stub — implementation pending. Exists so Vite can resolve the import for tests.
export function createProvider(_config: Record<string, unknown>): never {
  throw new Error('createProvider is not implemented yet');
}
